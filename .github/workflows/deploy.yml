name: Deploy

on:
  push:
    tags: ['v*.*.*']

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 1. Determinar Directorio Destino según el Tag
      - name: Determine Target Directory
        id: target
        run: |
          TAG="${{ github.ref_name }}"
          echo "Analizando tag: $TAG"
          
          echo "TARGET_DIR=${{ secrets.PROJECT_PATH }}" >> $GITHUB_ENV
          echo "BRANCH_NAME=main" >> $GITHUB_ENV
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV

      # 4. Ejecutar comandos SSH (Nativo)
      - name: Deploy to VM via Native SSH
        run: |
          # 1. Configurar Llave SSH
          mkdir -p ~/.ssh/
          # Usamos printf para asegurar que se escriba correctamente con un salto de línea final
          printf "%s\n" "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # Validar que la llave sea válida (esto fallará si el formato es incorrecto)
          # No imprimimos la llave, solo verificamos si ssh-keygen puede leerla
          if ! ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null; then
            echo "❌ Error: La llave SSH privada tiene un formato inválido o está corrupta."
            echo "ℹ️ Asegúrate de copiar TODO el contenido, incluyendo '-----BEGIN...' y '-----END...'."
            echo "ℹ️ Verifica que el secreto en GitHub no tenga espacios extra al inicio o final."
            exit 1
          fi
          
          # 2. Definir Variables
          TARGET_DIR="${{ secrets.PROJECT_PATH }}"
          BRANCH_NAME="main"
          TAG_NAME="${{ github.ref_name }}"

          echo "🚀 Conectando a ${{ secrets.SSH_IP_SERVER }}..."

          # 3. Ejecutar Script Remoto
          # Al haber configurado la llave en ~/.ssh/id_rsa, ssh la usará automáticamente.
          # -o StrictHostKeyChecking=no: Evita el prompt de "Are you sure you want to continue connecting?"
          ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER_NAME }}@${{ secrets.SSH_IP_SERVER }} \
            "TARGET_DIR='$TARGET_DIR' BRANCH_NAME='$BRANCH_NAME' TAG_NAME='$TAG_NAME' bash -s" << 'EOF'
            set -e

            # Validar variables
            if [ -z "$TARGET_DIR" ]; then
              echo "❌ Error: TARGET_DIR no definido."
              exit 1
            fi

            # Entrar al directorio
            cd "$TARGET_DIR"

            echo "🚀 Usuario autenticado: $(whoami)"
            echo "📦 Actualizando código con tag: $TAG_NAME"
            echo "🌿 Rama a usar: $BRANCH_NAME"
            
            # Git Checkout
            git checkout "$BRANCH_NAME"
            git fetch --prune --prune-tags --force
            git fetch origin tag "$TAG_NAME"
            git checkout --force "$TAG_NAME"
            
            # Dependencias Backend
            echo "🔧 Composer Install..."
            php composer.phar install --no-interaction --prefer-dist --optimize-autoloader --no-dev
            
            # Migrar Datos
            echo "🗄️ Migrando base de datos..."
            php artisan migrate --force
            
            # Limpieza de caché
            echo "🧹 Limpieza de caché..."
            php artisan optimize
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            
            if php artisan list | grep -q 'filament:optimize'; then
              php artisan filament:optimize
            fi

            # Permisos
            sudo chgrp -R www-data storage bootstrap/cache
            sudo chmod -R 775 storage bootstrap/cache

            echo "✅ Despliegue completado!"
          EOF
